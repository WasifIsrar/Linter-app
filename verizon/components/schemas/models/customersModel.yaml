title: customersResponse
properties:
  id:
    $ref: ../id.yaml
  created:
    $ref: ../created.yaml
  modified:
    $ref: ../modified.yaml
  creator:
    $ref: ../creator.yaml
  modifier:
    $ref: ../modifier.yaml
  login:
    type: string
    description: The ID of the Login that owns this resource.
  merchant:
    type: string
    description: The ID of the Merchant associated with this Customer.
  entity:
    type: string
    description: The ID of the Entity that owns this resource.
  first:
    type: string
    description: The first name associated with this Customer.
  middle:
    type: string
    description: The middle name associated with this Customer.
  last:
    type: string
    description: The last name associated with this Customer.
  company:
    type: string
    description: The name of the company associated with this Customer.
  email:
    type: string
    description: The email address of this Customer.
  fax:
    type: string
    description: "The fax number associated with this Customer. \nThis field\
      \ is stored as a text string and must be between 10 and 15 characters\
      \ long."
  phone:
    type: string
    description: "The phone number associated with this Transaction. \nThis\
      \ field is stored as a text string and must be between 10 and 15 characters\
      \ long."
  country:
    $ref: ../models/countryModel.yaml
    description: "The country associated with this Customer. \nValid values\
      \ for this field is the 3-letter ISO code for the country."
  zip:
    type: string
    description: "The ZIP code in the address associated with this Customer.\
      \ \nThis field is stored as a text string and must be between 1 and 20\
      \ characters long."
  state:
    $ref: ../models/stateModel.yaml
  city:
    type: string
    description: "The name of the city in the address associated with this Customer.\
      \ \nThis field is stored as a text string and must be between 1 and 500\
      \ characters long."
  address2:
    type: string
    description: "The second line of the address associated with this Customer.\
      \ \nThis field is stored as a text string and must be between 1 and 500\
      \ characters long."
  address1:
    type: string
    description: "The first line of the address associated with this Customer.\
      \ \nThis field is stored as a text string and must be between 1 and 500\
      \ characters long."
  shippingFirst:
    type: string
    description: The first name associated with this Customer's shipping information.
  shippingMiddle:
    type: string
    description: The middle name associated with this Customer's shipping information.
  shippingLast:
    type: string
    description: The last name associated with this Customer's shipping information.
  shippingCompany:
    type: string
    description: The name of the company associated with this Customer's shipping information.
  shippingAddress1:
    type: string
    description: "The first line of the address associated with this Customer's\
      \ shipping information. \nThis field is stored as a text string and must be between\
      \ 1 and 500 characters long."
  shippingAddress2:
    type: string
    description: "The second line of the address associated with this Customer's\
      \ shipping information. \nThis field is stored as a text string and must be between\
      \ 1 and 500 characters long."
  shippingCity:
    type: string
    description: "The name of the city associated with this Customer's shipping\
      \ information. \nThis field is stored as a text string and must be between\
      \ 1 and 500 characters long."
  shippingState:
    $ref: ../models/stateModel.yaml
  shippingZip:
    type: string
    description: "The ZIP code associated with this Customer's shipping\
      \ information. \nThis field is stored as a text string and must be between\
      \ 1 and 20 characters long."
  shippingCountry:
    $ref: ../models/countryModel.yaml
    description: "The country associated with this Customer's shipping information. \nValid\
      \ values for this field is the 3-letter ISO code for the country."
  shippingPhone:
    type: string
    description: "The phone number associated with this Customer's shipping information.\
      \ \nThis field is stored as a text string and must be between 10 and 15\
      \ characters long."
  shippingFax:
    type: string
    description: "The fax number associated with this Customer's shipping information. \n\
      This field is stored as a text string and must be between 10 and 15 characters\
      \ long."
  custom:
    type: string
    description: Custom, free-form field for client-supplied text. Must be between
      0 and 1,000 characters long.
  authTokenCustomer:
    type: string
    description: The customer reference from the authToken used for user authentication,
      if available.
  frozen:
    $ref: ../frozen.yaml
  inactive:
    $ref: ../inactive.yaml
