title: subscriptionResponseResult
properties:
  response:
    title: subscriptionResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/subscriptionsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
