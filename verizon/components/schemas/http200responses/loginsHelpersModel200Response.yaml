title: loginsHelpersResponseResult
properties:
  response:
    title: loginsHelpersResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/loginHelpersModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
