title: contactsResponseResult
properties:
  response:
    title: contactsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/contactsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
