title: taxFormRequestsResponseResult
properties:
  response:
    title: taxFormRequestsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/taxFormRequestsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
