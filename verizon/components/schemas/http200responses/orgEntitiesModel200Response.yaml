title: orgEntitiesResponseResult
properties:
  response:
    title: orgEntitiesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/orgEntitiesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
