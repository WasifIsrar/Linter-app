title: valueAddedServicesResponseResult
properties:
  response:
    title: valueAddedServicesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/valueAddedServicesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
