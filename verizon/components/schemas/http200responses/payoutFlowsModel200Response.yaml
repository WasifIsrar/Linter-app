title: payoutFlowsResponseResult
properties:
  response:
    title: payoutFlowsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/payoutFlowsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
