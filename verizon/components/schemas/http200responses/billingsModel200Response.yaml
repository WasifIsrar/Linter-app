title: billingsResponseResult
properties:
  response:
    title: billingsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/billingsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
