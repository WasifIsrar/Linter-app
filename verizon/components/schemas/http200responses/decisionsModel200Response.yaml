title: decisionsResponseResult
properties:
  response:
    title: decisionsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/decisionsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
