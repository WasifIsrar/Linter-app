title: reservesResponseResult
properties:
  response:
    title: reservesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/reservesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
