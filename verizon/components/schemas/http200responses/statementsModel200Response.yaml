title: statementsResponseResult
properties:
  response:
    title: statementsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/statementsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
