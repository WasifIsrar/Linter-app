title: parametersResponseResult
properties:
  response:
    title: parametersResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/parametersModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
