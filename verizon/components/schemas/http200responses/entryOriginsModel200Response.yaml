title: entryOriginsResponseResult
properties:
  response:
    title: entryOriginsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/entryOriginsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
