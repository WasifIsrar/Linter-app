title: tokensResponseResult
properties:
  response:
    title: tokensResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/tokensModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
