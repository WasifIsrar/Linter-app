title: secretsResponseResult
properties:
  response:
    title: secretsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/secretsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
