title: divisionsResponseResult
properties:
  response:
    title: divisionsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/divisionsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
