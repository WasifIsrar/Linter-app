title: entityRoutesResponseResult
properties:
  response:
    title: entityRoutesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/entityRoutesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
