title: terminalTxnResultsResponseResult
properties:
  response:
    title: terminalTxnResultsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/terminalTxnResultsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
