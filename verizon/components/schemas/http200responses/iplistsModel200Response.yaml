title: iplistsResponseResult
properties:
  response:
    title: iplistsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/iplistsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
