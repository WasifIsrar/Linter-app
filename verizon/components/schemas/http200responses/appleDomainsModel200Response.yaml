title: appleDomainsResponseResult
properties:
  response:
    title: appleDomainsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/appleDomainsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
