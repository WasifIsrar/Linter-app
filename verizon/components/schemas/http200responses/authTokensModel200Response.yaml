title: authTokensResponseResult
properties:
  response:
    title: authTokensResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/authTokensModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
