title: confirmCodesResponseResult
properties:
  response:
    title: confirmCodesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/confirmCodesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
