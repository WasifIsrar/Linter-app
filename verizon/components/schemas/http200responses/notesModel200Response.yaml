title: notesResponseResult
properties:
  response:
    title: notesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/notesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
