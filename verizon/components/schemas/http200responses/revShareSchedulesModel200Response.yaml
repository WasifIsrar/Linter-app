title: revShareSchedulesResponseResult
properties:
  response:
    title: revShareSchedulesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/revShareSchedulesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
