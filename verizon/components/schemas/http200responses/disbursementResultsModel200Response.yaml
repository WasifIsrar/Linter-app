title: disbursementResultsResponseResult
properties:
  response:
    title: disbursementResultsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/disbursementResultsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
