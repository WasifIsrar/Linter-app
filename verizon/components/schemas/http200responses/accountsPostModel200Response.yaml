title: accountsPostResponseResult
properties:
  response:
    title: accountsPostResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/accountsPostModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
