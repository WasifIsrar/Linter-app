title: disbursementsResponseResult
properties:
  response:
    title: disbursementsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/disbursementsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
