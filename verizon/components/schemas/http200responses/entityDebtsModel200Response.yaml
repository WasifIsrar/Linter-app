title: entityDebtsResponseResult
properties:
  response:
    title: entityDebtsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/entityDebtsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
