title: mfaRecoveryCodesResponseResult
properties:
  response:
    title: mfaRecoveryCodesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/mfaRecoveryCodesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
