title: profitShareRulesResponseResult
properties:
  response:
    title: profitShareRulesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/profitShareRulesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
