title: invoiceResultsResponseResult
properties:
  response:
    title: invoiceResultsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/invoiceResultModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
