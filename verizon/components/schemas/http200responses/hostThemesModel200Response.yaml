title: hostThemesResponseResult
properties:
  response:
    title: hostThemesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/hostThemesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
