title: txnSessionsResponseResult
properties:
  response:
    title: txnSessionsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/txnSessionsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
