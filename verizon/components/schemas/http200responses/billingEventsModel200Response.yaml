title: billingEventsResponseResult
properties:
  response:
    title: billingEventsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/billingEventsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
