title: decisionActionsResponseResult
properties:
  response:
    title: decisionActionsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/decisionActionsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
