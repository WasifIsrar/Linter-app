title: vasEfeOffersResponseResult
properties:
  response:
    title: vasEfeOffersResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/vasEfeOffersModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
