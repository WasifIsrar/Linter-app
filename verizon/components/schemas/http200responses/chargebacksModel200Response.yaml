title: chargebacksResponseResult
properties:
  response:
    title: chargebacksResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/chargebacksModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
