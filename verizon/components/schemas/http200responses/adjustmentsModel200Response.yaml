title: adjustmentsResponseResult
properties:
  response:
    title: adjustmentsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/adjustmentsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
