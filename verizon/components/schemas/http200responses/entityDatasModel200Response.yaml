title: entityDatasResponseResult
properties:
  response:
    title: entityDatasResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/entityDataModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
