title: billingModifiersResponseResult
properties:
  response:
    title: billingModifiersResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/billingModifiersModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
