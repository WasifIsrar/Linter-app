title: entityReservesResponseResult
properties:
  response:
    title: entityReservesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/entityReservesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
