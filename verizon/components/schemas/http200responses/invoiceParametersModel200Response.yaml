title: invoiceParametersResponseResult
properties:
  response:
    title: invoiceParametersResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/invoiceParametersModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
