title: assessmentsResponseResult
properties:
  response:
    title: assessmentsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/assessmentsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
