title: reportsResponseResult
properties:
  response:
    title: reportsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/reportsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
