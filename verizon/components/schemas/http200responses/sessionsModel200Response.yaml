title: sessionsResponseResult
properties:
  response:
    title: sessionsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/sessionModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
