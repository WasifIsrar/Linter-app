title: mappingsResponseResult
properties:
  response:
    title: mappingsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/mappingsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
