title: currencyRatesResponseResult
properties:
  response:
    title: currencyRatesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/currencyRatesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
