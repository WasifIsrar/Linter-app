title: reserveEntriesResponseResult
properties:
  response:
    title: reserveEntriesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/reserveEntriesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
