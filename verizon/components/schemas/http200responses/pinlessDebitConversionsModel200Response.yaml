title: pinlessDebitConversionsResponseResult
properties:
  response:
    title: pinlessDebitConversionsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/pinlessDebitConversionsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
