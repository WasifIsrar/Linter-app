title: orgFlowAction
type: string
description: |
  The action to take in relation to the Entity being processed.

  <details>
  <summary>Valid Values</summary>

    - `add` - **Add the referenced Entity to the referenced Org.**
    - `remove` - **Remove the referenced Entity from the referenced Org.**

  </details>
enum:
- add
- remove
x-enum-elements:
- name: Add
  description: Add the referenced Entity to the referenced Org.
- name: Remove
  description: Remove the referenced Entity from the referenced Org.