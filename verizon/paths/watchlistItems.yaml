get:
  description: Query a WatchlistItem. A WatchlistItem is a set of attributes of a
    person or entity of interest.
  parameters:
  - $ref: ../components/schemas/requestHeaderRequestTokensParams.yaml
  - $ref: ../components/schemas/requestHeaderSearchParams.yaml
  - $ref: ../components/schemas/requestHeaderTotalsParams.yaml
  - $ref: ../components/schemas/requestQueryPageNumberParams.yaml
  - $ref: ../components/schemas/requestQueryPageLimitParams.yaml
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/http200responses/watchlistItemsModel200Response.yaml
      description: Response for query on watchlistItems resource
    '400':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/error-four-hundred.yaml
      description: Error
  tags:
  - Watchlist Items
  - Facilitator
  operationId: getWatchlistItems
post:
  description: Create a WatchlistItem. A WatchlistItem is a set of attributes of a
    person or entity of interest.
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/models/watchlistItemsPostRequestModel.yaml
    required: true
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/http200responses/watchlistItemsModel200Response.yaml
      description: Response for create on watchlistItems resource
    '400':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/error-four-hundred.yaml
      description: Error
  tags:
  - Watchlist Items
  - Facilitator
  operationId: postWatchlistItems
  parameters:
  - $ref: ../components/schemas/requestHeaderRequestTokensParams.yaml
