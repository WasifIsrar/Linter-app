get:
  description: "Query a Watchlist. A Watchlist is a list of WatchlistItems. \nWatchlistItems\
    \ will be associated with a Watchlist and can be used in Decisions."
  parameters:
  - $ref: ../components/schemas/requestHeaderRequestTokensParams.yaml
  - $ref: ../components/schemas/requestHeaderSearchParams.yaml
  - $ref: ../components/schemas/requestHeaderTotalsParams.yaml
  - $ref: ../components/schemas/requestQueryPageNumberParams.yaml
  - $ref: ../components/schemas/requestQueryPageLimitParams.yaml
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/http200responses/watchlistsModel200Response.yaml
      description: Response for query on watchlists resource
    '400':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/error-four-hundred.yaml
      description: Error
  tags:
  - Watchlists
  - Facilitator
  operationId: getWatchlists
post:
  description: "Create a Watchlist. A Watchlist is a list of WatchlistItems. \nWatchlistItems\
    \ will be associated with a Watchlist and can be used in Decisions."
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/models/watchlistsPostRequestModel.yaml
    required: true
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/http200responses/watchlistsModel200Response.yaml
      description: Response for create on watchlists resource
    '400':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/error-four-hundred.yaml
      description: Error
  tags:
  - Watchlists
  - Facilitator
  operationId: postWatchlists
  parameters:
  - $ref: ../components/schemas/requestHeaderRequestTokensParams.yaml
