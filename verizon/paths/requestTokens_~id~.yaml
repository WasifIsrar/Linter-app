get:
  description: "Query a RequestToken. \nA RequestToken represents a method of assuring\
    \ a request is only processed once. Each RequestToken expires automatically after\
    \ 48 hours of being created."
  parameters:
  - $ref: ../components/schemas/requestHeaderRequestTokensParams.yaml
  - description: The ID of this resource.
    explode: false
    in: path
    name: id
    required: true
    schema:
      type: string
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/http200responses/requestTokensModel200Response.yaml
      description: Response for query on requestTokens resource
    '400':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/error-four-hundred.yaml
      description: Error
  tags:
  - Request Tokens
  - Facilitator
  operationId: getRequestTokensId
delete:
  description: "Delete a RequestToken. \nA RequestToken represents a method of assuring\
    \ a request is only processed once. Each RequestToken expires automatically after\
    \ 48 hours of being created."
  parameters:
  - $ref: ../components/schemas/requestHeaderRequestTokensParams.yaml
  - description: The ID of this resource.
    explode: false
    in: path
    name: id
    required: true
    schema:
      type: string
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/http200responses/requestTokensModel200Response.yaml
      description: Response for delete on requestTokens resource
    '400':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/error-four-hundred.yaml
      description: Error
  tags:
  - Request Tokens
  - Facilitator
  operationId: deleteRequestTokensId
