get:
  description: Query reportItems.
  parameters:
  - $ref: ../components/schemas/requestHeaderRequestTokensParams.yaml
  - description: "Set this header to filter or sort the list of resources that the\
      \ method returns. \nFor example, to find all resources that have a property\
      \ of 'public' set to '1', set this header to 'public[equals]=1'. You can use\
      \ the operators 'equals', 'greater', 'less', 'like', 'in' and 'sort'. To sort\
      \ the resources, use the keywords 'asc' (for an ascending sort) or 'desc' (for\
      \ a descending sort).\nYou may also use explicit 'and/or' querying by passing\
      \ arrays of or and arrays of and parameters. For example, to find all resources\
      \ that have a property of 'public' set to '1' or a property of 'active' set\
      \ to '0', set this header to 'or[][public][equals]=1&or[][active][equals]=0'."
    explode: false
    in: header
    x-unique-name: searchFilter
    name: search
    required: false
    schema:
      type: string
    example: created[equals]=2025-01-01
  - $ref: ../components/schemas/requestQueryPageNumberParams.yaml
  - $ref: ../components/schemas/requestQueryPageLimitParams.yaml
  responses:
    '200':
      content:
        application/json:
          examples:
            fetch all reportItems:
              $ref: ../components/schemas/samples/reportItemsJson.yaml
          schema:
            $ref: ../components/schemas/http200responses/reportItemsModel200Response.yaml
      description: Response for query on reportItems resource
    '400':
      content:
        application/json:
          examples:
            Fetch all reportItems:
              $ref: ../components/schemas/samples/errors400Json.yaml
          schema:
            $ref: ../components/schemas/error-four-hundred.yaml
      description: Error
  summary: Retrieve all Report Items.
  tags:
  - Report Items
  - Facilitator
  operationId: getReportItems
post:
  description: Create a Report Item.
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/models/reportItemsPostRequestModel.yaml
    description: Create Report Item Request
    required: true
  responses:
    '200':
      content:
        application/json:
          examples:
            Create reportItems:
              $ref: ../components/schemas/samples/reportItemsJson.yaml
          schema:
            $ref: ../components/schemas/http200responses/reportItemsModel200Response.yaml
      description: Response for create on reportItems resource
    '400':
      content:
        application/json:
          examples:
            Fetch all reportItems:
              $ref: ../components/schemas/samples/errors400Json.yaml
          schema:
            $ref: ../components/schemas/error-four-hundred.yaml
      description: Error
  summary: Create a new Report Item
  tags:
  - Report Items
  - Facilitator
  operationId: postReportItems
  parameters:
  - $ref: ../components/schemas/requestHeaderRequestTokensParams.yaml
