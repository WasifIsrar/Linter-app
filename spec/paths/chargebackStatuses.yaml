get:
  description: Query ChargebackStatuses. A ChargebackStatuses is the logged status
    changes of a Chargeback; The resource for each Chargeback gives details of the
    Chargeback, including the reason why it occurred and the date when it occurred.
  parameters:
  - $ref: ../components/schemas/requestHeaderRequestTokensParams.yaml
  - $ref: ../components/schemas/requestHeaderSearchParams.yaml
  - $ref: ../components/schemas/requestHeaderTotalsParams.yaml
  - $ref: ../components/schemas/requestQueryPageNumberParams.yaml
  - $ref: ../components/schemas/requestQueryPageLimitParams.yaml
  responses:
    '200':
      content:
        application/json:
          examples:
            Fetch all chargebackStatuses:
              $ref: ../components/schemas/samples/chargebackStatusesJson.yaml
          schema:
            $ref: ../components/schemas/http200responses/chargebackStatusesModel200Response.yaml
      description: Response for query on chargebackStatuses resource
    '400':
      content:
        application/json:
          examples:
            Fetch all chargebackStatuses:
              $ref: ../components/schemas/samples/errors400Json.yaml
          schema:
            $ref: ../components/schemas/error-four-hundred.yaml
      description: Error
  summary: Retrieve all Chargeback Statuses.
  tags:
  - Chargeback Statuses
  - Merchant
  - Facilitator
  - Partner
  operationId: getChargebackStatuses
post:
  description: create chargebackStatuses. A ChargebackStatuses is the logged status
    changes of a Chargeback; The resource for each Chargeback gives details of the
    Chargeback, including the reason why it occurred and the date when it occurred.
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/models/chargebackStatusesPostRequestModel.yaml
    required: true
  responses:
    '200':
      content:
        application/json:
          examples:
            Create chargebackStatuses:
              $ref: ../components/schemas/samples/chargebackStatusesJson.yaml
          schema:
            $ref: ../components/schemas/http200responses/chargebackStatusesModel200Response.yaml
      description: Response for create on chargebackStatuses resource
    '400':
      content:
        application/json:
          examples:
            Create chargebackStatuses:
              $ref: ../components/schemas/samples/errors400Json.yaml
          schema:
            $ref: ../components/schemas/error-four-hundred.yaml
      description: Error
  tags:
  - Chargeback Statuses
  - Facilitator
  operationId: postChargebackStatuses
  parameters:
  - $ref: ../components/schemas/requestHeaderRequestTokensParams.yaml
