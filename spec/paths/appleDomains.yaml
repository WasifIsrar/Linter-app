get:
  description: Query a appleDomains resource.
  parameters:
  - $ref: ../components/schemas/requestHeaderRequestTokensParams.yaml
  - $ref: ../components/schemas/requestHeaderSearchParams.yaml
  - $ref: ../components/schemas/requestHeaderTotalsParams.yaml
  - $ref: ../components/schemas/requestQueryPageNumberParams.yaml
  - $ref: ../components/schemas/requestQueryPageLimitParams.yaml
  responses:
    '200':
      content:
        application/json:
          examples:
            Fetch all appleDomains:
              $ref: ../components/schemas/samples/appleDomainsJson.yaml
          schema:
            $ref: ../components/schemas/http200responses/appleDomainsModel200Response.yaml
      description: Response for querying and retrieving all appleDomains.
    '400':
      content:
        application/json:
          examples:
            Fetch all appleDomains:
              $ref: ../components/schemas/samples/errors400Json.yaml
          schema:
            $ref: ../components/schemas/error-four-hundred.yaml
      description: Error
  summary: Retrieve all appleDomains.
  tags:
  - Apple Domains
  - Merchant
  - Partner
  operationId: getAppleDomains
