get:
  description: 'Query a Secret.

    A Secret represents either a key used to decrypt data or an indicator of which
    key to be used to decrypt data.'
  parameters:
  - $ref: ../components/schemas/requestHeaderRequestTokensParams.yaml
  - $ref: ../components/schemas/requestHeaderSearchParams.yaml
  - $ref: ../components/schemas/requestHeaderTotalsParams.yaml
  - $ref: ../components/schemas/requestQueryPageNumberParams.yaml
  - $ref: ../components/schemas/requestQueryPageLimitParams.yaml
  responses:
    '200':
      content:
        application/json:
          examples:
            Fetch all secrets:
              $ref: ../components/schemas/samples/secretsJson.yaml
          schema:
            $ref: ../components/schemas/http200responses/secretsModel200Response.yaml
      description: Response for query on secrets resource
    '400':
      content:
        application/json:
          examples:
            Fetch all secrets:
              $ref: ../components/schemas/samples/errors400Json.yaml
          schema:
            $ref: ../components/schemas/error-four-hundred.yaml
      description: Error
  summary: Retrieve all Secrets.
  tags:
  - Secrets
  - Merchant
  - Facilitator
  - Partner
  operationId: getSecrets
post:
  description: 'Create a Secret.

    A Secret represents either a key used to decrypt data or an indicator of which
    key to be used to decrypt data.'
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/models/secretsPostRequestModel.yaml
    description: Create Secret Request
    required: true
  responses:
    '200':
      content:
        application/json:
          examples:
            Create secrets:
              $ref: ../components/schemas/samples/secretsJson.yaml
          schema:
            $ref: ../components/schemas/http200responses/secretsModel200Response.yaml
      description: Response for create on secrets resource
    '400':
      content:
        application/json:
          examples:
            Fetch all secrets:
              $ref: ../components/schemas/samples/errors400Json.yaml
          schema:
            $ref: ../components/schemas/error-four-hundred.yaml
      description: Error
  summary: Create a new Secret.
  tags:
  - Secrets
  - Merchant
  - Facilitator
  - Partner
  operationId: postSecrets
  parameters:
  - $ref: ../components/schemas/requestHeaderRequestTokensParams.yaml
