title: profitShareResultsResponse
properties:
  id:
    $ref: ../id.yaml
  created:
    $ref: ../created.yaml
  modified:
    $ref: ../modified.yaml
  creator:
    $ref: ../creator.yaml
  modifier:
    $ref: ../modifier.yaml
  profitShare:
    type: string
    description: The identifier of the ProfitShare that this ProfitShareResult refers
      to.
  entry:
    type: string
    description: The identifier of the Entry that this ProfitShareResult refers to.
  amount:
    type: number
    format: double
    description: The amount that could not be shared between entities, This field is specified in cents(up to three decimal points)
  message:
    type: string
    description: "A message regarding the failure of the ProfitShare process. \nThis\
      \ field is stored as a text string and must be between 0 and 200 characters\
      \ long."
