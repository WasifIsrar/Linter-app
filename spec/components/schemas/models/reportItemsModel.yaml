title: reportItemsResponse
properties:
  id:
    $ref: ../id.yaml
  created:
    $ref: ../created.yaml
  modified:
    $ref: ../modified.yaml
  creator:
    $ref: ../creator.yaml
  modifier:
    $ref: ../modifier.yaml
  report:
    type: string
    description: Report table Id.
  search:
    type: string
    description: search criteria eg. id[greater] = 0
  totals:
    type: string
    description: Total eg.sum[]=total.
  expand:
    type: string
    description: By default, when a resource points to another resource, the ID of
      the parent resource will be included in the child resource. To return the full
      object you can use expand.
  expandSearch:
    type: string
    description: You can compare key values against pre-defined criteria to return
      resources based on the operator.
  resource:
    $ref: ../enums/resourceModel.yaml
  inactive:
    $ref: ../inactive.yaml
  frozen:
    $ref: ../frozen.yaml
