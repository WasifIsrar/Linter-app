title: reportResultsResponse
properties:
  id:
    $ref: ../id.yaml
  created:
    $ref: ../created.yaml
  modified:
    $ref: ../modified.yaml
  creator:
    $ref: ../creator.yaml
  modifier:
    $ref: ../modifier.yaml
  report:
    type: string
    description: Report table Id.
  type:
    title: reportResultType
    type: string
    description: Format types (JSON, XML, etc).
  documentType:
    $ref: ../enums/reportDocumentTypeModel.yaml
  original:
    type: integer
    description: TBD
  status:
    $ref: ../enums/reportResultStatusModel.yaml
  effective:
    type: integer
    format: int64
    description: The date format as below and it is the effective date of the report
      query and it is taking all the characters except last one and appending 0 to
      end. and even DB values have the same  format.
  inactive:
    $ref: ../inactive.yaml
  frozen:
    $ref: ../frozen.yaml
