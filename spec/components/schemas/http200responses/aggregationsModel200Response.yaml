title: aggregationsResponseResult
properties:
  response:
    title: aggregationsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/aggregationsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
