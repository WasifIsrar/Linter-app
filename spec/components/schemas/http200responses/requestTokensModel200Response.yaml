title: requestTokensResponseResult
properties:
  response:
    title: requestTokensResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/requestTokensModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
