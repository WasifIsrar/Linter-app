title: messagesResponseResult
properties:
  response:
    title: messagesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/messagesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
