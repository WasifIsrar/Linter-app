title: changeRequestsResponseResult
properties:
  response:
    title: changeRequestsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/changeRequestModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
