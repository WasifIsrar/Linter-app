title: vendorsResponseResult
properties:
  response:
    title: vendorsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/vendorsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
