title: reportItemsResponseResult
properties:
  response:
    title: reportItemsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/reportItemsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
