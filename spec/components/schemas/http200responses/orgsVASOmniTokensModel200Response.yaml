title: orgsVASOmniTokensResponseResult
properties:
  response:
    title: orgsVASOmniTokensResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/orgsVASOmniTokensModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
