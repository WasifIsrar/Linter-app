title: sessionsPostResponseResult
properties:
  response:
    title: sessionsPostResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/sessionPostModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
