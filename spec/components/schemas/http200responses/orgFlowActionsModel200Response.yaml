title: orgFlowActionsResponseResult
properties:
  response:
    title: orgFlowActionsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/orgFlowActionsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
