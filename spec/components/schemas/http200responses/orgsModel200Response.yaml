title: orgsResponseResult
properties:
  response:
    title: orgsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/orgsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
