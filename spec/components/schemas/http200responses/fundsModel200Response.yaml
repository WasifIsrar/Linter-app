title: fundsResponseResult
properties:
  response:
    title: fundsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/fundsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
