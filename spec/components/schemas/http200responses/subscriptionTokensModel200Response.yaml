title: subscriptionTokensResponseResult
properties:
  response:
    title: subscriptionTokensResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/subscriptionTokensModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
