title: loginsResponseResult
properties:
  response:
    title: loginsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/loginsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
