title: txnResultsResponseResult
properties:
  response:
    title: txnResultsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/txnResultsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
