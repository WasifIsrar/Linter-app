title: txnDatasResponseResult
properties:
  response:
    title: txnDatasResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/txnsDatasModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
