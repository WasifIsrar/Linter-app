title: customersResponseResult
properties:
  response:
    title: customersResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/customersModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
