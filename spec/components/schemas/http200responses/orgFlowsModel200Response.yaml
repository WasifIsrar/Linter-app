title: orgFlowsResponseResult
properties:
  response:
    title: orgFlowsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/orgFlowsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
