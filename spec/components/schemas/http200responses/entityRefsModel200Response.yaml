title: entityRefsResponseResult
properties:
  response:
    title: entityRefsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/entityRefsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
