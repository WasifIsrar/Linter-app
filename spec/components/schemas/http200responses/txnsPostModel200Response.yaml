title: txnsPostResponseResult
properties:
  response:
    title: txnsPostResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/txnsPostModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
