title: fundingParametersResponseResult
properties:
  response:
    title: fundingParametersResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/fundingParametersModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
