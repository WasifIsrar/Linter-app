title: aggregationResultsGroupsResponseResult
properties:
  response:
    title: aggregationResultsGroupsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/aggregationResultGroupsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
