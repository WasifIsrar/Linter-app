title: batchesResponseResult
properties:
  response:
    title: batchesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/batchesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
