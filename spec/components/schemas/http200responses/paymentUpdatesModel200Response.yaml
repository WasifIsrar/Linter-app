title: paymentUpdatesResponseResult
properties:
  response:
    title: paymentUpdatesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/paymentUpdatesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
