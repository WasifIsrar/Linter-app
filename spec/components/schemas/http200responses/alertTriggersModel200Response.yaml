title: alertTriggersResponseResult
properties:
  response:
    title: alertTriggersResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/alertTriggersModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
