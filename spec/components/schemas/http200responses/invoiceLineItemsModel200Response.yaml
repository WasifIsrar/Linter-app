title: invoiceLineItemsResponseResult
properties:
  response:
    title: invoiceLineItemsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/invoiceLineItemsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
