title: statementEntriesResponseResult
properties:
  response:
    title: statementEntriesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/statementEntriesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
