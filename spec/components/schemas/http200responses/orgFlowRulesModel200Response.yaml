title: orgFlowRulesResponseResult
properties:
  response:
    title: orgFlowRulesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/orgFlowRulesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml