title: credentialsResponseResult
properties:
  response:
    title: credentialsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/credentialsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
