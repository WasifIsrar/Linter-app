title: paymentUpdateGroupsResponseResult
properties:
  response:
    title: paymentUpdateGroupsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/paymentUpdateGroupsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
