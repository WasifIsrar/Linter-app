title: verificationRefsResponseResult
properties:
  response:
    title: verificationRefsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/verificationRefsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
