title: embeddedFinanceResponseResult
properties:
  response:
    title: embeddedFinanceResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/embeddedFinanceModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
