title: accountRefsResponseResult
properties:
  response:
    title: accountRefsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/accountReferenceModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
