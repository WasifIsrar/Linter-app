title: feeModifiersResponseResult
properties:
  response:
    title: feeModifiersResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/feeModifiersModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
