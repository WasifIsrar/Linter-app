title: entityTermsResponseResult
properties:
  response:
    title: entityTermsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/entityTermsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
