title: terminalTxnRefResponseResult
properties:
  response:
    title: terminalTxnRefResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/terminalTxnRefsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
