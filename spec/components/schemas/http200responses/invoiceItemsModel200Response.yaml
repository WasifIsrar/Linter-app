title: invoiceItemsResponseResult
properties:
  response:
    title: invoiceItemsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/invoiceItemsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
