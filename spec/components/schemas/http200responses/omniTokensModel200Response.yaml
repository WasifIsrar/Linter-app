title: omniTokensResponseResult
properties:
  response:
    title: omniTokensResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/omniTokensModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
