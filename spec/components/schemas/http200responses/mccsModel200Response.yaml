title: mccsResponseResult
properties:
  response:
    title: mccsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/mccsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
