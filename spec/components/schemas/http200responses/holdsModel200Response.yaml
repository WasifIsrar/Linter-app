title: holdsResponseResult
properties:
  response:
    title: holdsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/holdsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
