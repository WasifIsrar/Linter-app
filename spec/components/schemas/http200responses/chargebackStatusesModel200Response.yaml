title: chargebackStatusesResponseResult
properties:
  response:
    title: chargebackStatusesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/chargebackStatusesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
