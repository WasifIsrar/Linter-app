title: accountVerificationResultsResponseResult
properties:
  response:
    title: accountVerificationResultsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/queryAccountVerificationResourceModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
