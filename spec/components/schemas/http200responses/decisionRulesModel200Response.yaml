title: decisionRulesResponseResult
properties:
  response:
    title: decisionRulesResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/decisionRulesModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
