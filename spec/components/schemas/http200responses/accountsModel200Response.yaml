title: accountsResponseResult
properties:
  response:
    title: accountsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/accountsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
