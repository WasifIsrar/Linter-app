title: txnsResponseResult
properties:
  response:
    title: txnsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/txnsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
