title: merchantsResponseResult
properties:
  response:
    title: merchantsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/merchantsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
