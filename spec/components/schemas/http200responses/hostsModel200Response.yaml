title: hostsResponseResult
properties:
  response:
    title: hostsResponseItem
    properties:
      data:
        description: The data object that the API returns for this request.
        items:
          $ref: ../models/hostsModel.yaml
      details:
        $ref: ../details.yaml
      errors:
        $ref: ../error.yaml
